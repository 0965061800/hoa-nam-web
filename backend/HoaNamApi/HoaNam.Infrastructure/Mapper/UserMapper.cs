namespace HoaNam.Infrastructure.Mapper
{
	public static class UserMapper
	{
		//public static AppUser ToDomain(AppIdentityUser identityUser, IList<string> roles)
		//{
		//	var profile = new Profile(identityUser.FirstName, identityUser.AvatarUrl);
		//	var id = identityUser.Id;

		//	if (roles.Contains("Admin"))
		//		return new Admin(id, profile);

		//	return new AppUser(id, profile); // default domain user
		//}
	}
}
